class Vector2 {
    constructor(public x: number, public y: number) { }

    // touchesRect(rect: Rect) {
    //     return (this.x > a.x1 && this.x < a.x2 && this.y > a.y1 && this.y < a.y2)
    // }
}

export {
    Vector2
}

